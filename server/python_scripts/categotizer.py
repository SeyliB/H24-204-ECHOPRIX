import sys

# Retrieve the title from command line argument
aaa = sys.argv[1]

# Process the received title (example: return a modified string)
processed_data = aaa.upper()  # Example processing (convert title to uppercase)

# Print the processed data (output will be captured by Node.js)
print(processed_data)
